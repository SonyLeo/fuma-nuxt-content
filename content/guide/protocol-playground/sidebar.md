---
title: Sidebar Contract
description: 验证剩余页面经过 meta 语法整理后的排序结果。
sectionLabel: Guide
---

这个页面配合 `routing.md` 一起验证 `z...a`。

## 预期

在 `overview` 被显式消费、`archive` 被排除之后，剩余页面应按反向顺序进入 sidebar：

1. `Sidebar Contract`
2. `Routing Contract`

如果后续我们继续补更完整的 Fumadocs 语法，这个目录可以继续作为回归样本。
